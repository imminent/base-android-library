package com.imminentmeals.android.base.ui;

import org.junit.Test;
import org.junit.runner.RunWith;

import com.imminentmeals.android.base.ActionBarSherlockTestRunner;

/**
 * <p>Test suite for the Home Activity.</p>
 * @author Dandre Allison
 */
@RunWith(ActionBarSherlockTestRunner.class)
public class HomeActivityTest {

    @Test
    public void testActionBarDisplay() throws Exception {
        /*// given
        HomeActivity activity_under_test = new HomeActivity();

        // when
        activity_under_test.onCreate(Bundle.EMPTY);

        // then
        assertThat(activity_under_test.getString(R.string.title_app)).isEqualTo("Home Screen");
        assertThat(activity_under_test.getSupportActionBar())
            .isShowing()
            .hasTitle(R.string.title_home_screen)
            .hasDisplayOptions(DISPLAY_SHOW_HOME|DISPLAY_USE_LOGO|DISPLAY_SHOW_TITLE);*/
    }
}
